@using PheoBakery.Models;
@model IEnumerable<SANPHAM>

<div class="container">
    <div class="product-top">
        <div class="product-one">
            @foreach (var item in Model.Take(10).OrderBy(n => n.DONGIA))
            {
                <div class="col-md-3 product-left">
                    <div class="product-main simpleCart_shelfItem">
                        @*<a href="@Url.RouteUrl("XemChiTiet" ,new {@MASP = item.MASP, @TENSP = item.TENSP})" class="mask">
                                <img class="img-responsive zoom-img" src="@Url.Content("~/Content/ckfinder/core/connector/aspx/" + Model.ElementAt(0).HINHANH)" alt="" />
                            </a>*@
                        <div class="product-bottom">
                            <h3> @item.TENSP</h3>
                            @*<p>@Html.Raw(item.MoTa)</p>*@
                            <h4><a class="item_add" href="@Url.Action("ThemGioHang", "GioHang", new { @MASP = item.MASP, @strUrl= Request.Url.ToString() }) "></a> <span class="item_price">@item.DONGIA.Value.ToString("#,##")</span></h4>

                        </div>
                        <div class="srch">
                            <span>-50%</span>
                        </div>
                    </div>
                </div>
            }
            <div class="clearfix"></div>
        </div>
    </div>
</div>
