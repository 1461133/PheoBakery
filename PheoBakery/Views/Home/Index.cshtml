@using PheoBakery.Models;
@model IEnumerable<SANPHAM>
@{
    ViewBag.Title = "Homepage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    IEnumerable<SANPHAM> lstQAM = (IEnumerable<SANPHAM>)ViewBag.lstQAM;
    IEnumerable<SANPHAM> lstMPM = (IEnumerable<SANPHAM>)ViewBag.lstMPM;
    IEnumerable<SANPHAM> lstPKM = (IEnumerable<SANPHAM>)ViewBag.lstPKM;
}

<body>
    <div id="warp">
        <div id="main" class="container_16">
            @{Html.RenderAction("Slider", "MyComponent");}
            @{Html.RenderAction("SpecialCake", "MyComponent");}
            @{Html.RenderAction("NewCake", "MyComponent");}

            @{ Html.RenderAction("HienThiSanPhamHot", "SANPHAMs"); }

            @{Html.RenderAction("InforCake", "MyComponent");}
            @{Html.RenderAction("Infor", "MyComponent");}
            @{Html.RenderAction("Footer", "MyComponent");}
        </div>
    </div>
</body>
<style>
    .h1 {
        text-align: center;
        font-weight: bold;
        font-size: 2em;
        margin: 10px;
        padding: 10px;
    }
</style>